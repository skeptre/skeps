# Optional — copy only if you need to override defaults.
# The app runs without any .env file (see src/config/site.ts).

# Canonical site URL for metadata, sitemap, and Open Graph (default: https://aliskeps.com)
# NEXT_PUBLIC_SITE_URL=https://aliskeps.com
